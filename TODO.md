# TODO: Add Routing for Login and Dashboard Pages

- [x] Create routing structure in `src/routes.jsx` with default route to `/login` for `LoginPage` and `/dashboard` for `DashboardPage`
- [x] Update `src/App.js` to wrap the app with `BrowserRouter` and render the routes from `routes.jsx`
- [x] Modify `src/pages/LoginPage.jsx` to use `useNavigate` and navigate to `/dashboard` on login button click
